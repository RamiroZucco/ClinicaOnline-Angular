import { HighlightEstadoDirective } from './highlight-estado.directive';

describe('HighlightEstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightEstadoDirective();
    expect(directive).toBeTruthy();
  });
});
