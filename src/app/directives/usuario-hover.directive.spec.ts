import { UsuarioHoverDirective } from './usuario-hover.directive';

describe('UsuarioHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new UsuarioHoverDirective();
    expect(directive).toBeTruthy();
  });
});
