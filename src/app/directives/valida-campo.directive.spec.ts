import { ValidaCampoDirective } from './valida-campo.directive';

describe('ValidaCampoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaCampoDirective();
    expect(directive).toBeTruthy();
  });
});
