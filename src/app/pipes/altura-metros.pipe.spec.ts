import { AlturaMetrosPipe } from './altura-metros.pipe';

describe('AlturaMetrosPipe', () => {
  it('create an instance', () => {
    const pipe = new AlturaMetrosPipe();
    expect(pipe).toBeTruthy();
  });
});
